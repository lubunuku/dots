#!/bin/bash
case $1 in
	i)
	led=a ;;
	mic)
	led=e ;;
	power)
	led=0 ;;
	fn)
	led=6 ;;
esac

case $2 in
	off)
	state=0 ;;
	on)
	state=8 ;;
	blink)
	state=c ;;
esac

echo -n -e "\x$state$led" | dd of="/sys/kernel/debug/ec/ec0/io" bs=1 seek=12 count=1 conv=notrunc 2> /dev/null
